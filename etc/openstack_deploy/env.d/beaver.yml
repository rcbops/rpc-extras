---
component_skel:
  beaver:
    belongs_to:
      - beaver_all

container_skel:
  beaver_container:
    belongs_to:
      - log_containers
    contains:
      - beaver
    properties:
      service_name: beaver
      container_release: trusty
